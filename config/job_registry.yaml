# mini launchd job registry — single source of truth for "what's installed and can it trade".
# Update this whenever a job is added/paused/retired/reinstalled. For narrative history
# (incidents, decisions, dates), check git log on this file and deploy/mac-mini/ — this
# file itself is the quick-scan snapshot, not the story.
#
# order_capable: true means the job's code CAN submit a real broker order (grep-verified
# against AUTO_SUBMIT/place_order call sites — see 2026-07-29 audit). It does NOT mean the
# job currently will; check status + the .env flags for that.
# status: enabled | disabled (launchctl disable — persists across reboot/reinstall)
#
# generated: 2026-07-29 · reconciled 2026-08-04 (rrg-c18acc-poll fully retired) · 2026-08-02 against live launchctl list
# (retired ops-live-ta-poll/live-ta-kbar-sync; winbond→nightly-expert-digest;
#  added capitulation-oos-accumulate). Verified directly against mini
# (~/Library/LaunchAgents/*.plist + launchctl print-disabled), not from memory or docs.
# 2026-08-08: added dayflip-short-poll (order_capable, enabled since 2026-08-07 — was live
# on mini but missing from this SSOT list; verified against launchctl print-disabled + plist).

version: job-registry-v1

jobs:
  # --- Order layer ---
  # rrg-c18acc-poll RETIRED 2026-08-04 — strategy no longer adopted. plist/launcher/.command
  # deleted from repo + mini, label moved to install-launchd.sh RETIRED_LABELS (auto-uninstalls
  # on any future install run), launchctl disable kept as belt-and-braces.
  # src/order/c18acc_*.py kept for manual/backtest use. Backup:
  # goldenstocks-data/.retired-launchd-backup-20260804-c18acc/
  - label: leading-dip-poll
    category: order
    order_capable: true
    status: disabled
    disabled_since: "2026-07-27"
    schedule: "weekdays 09:00-13:30, every 5min"
    description: "Leading Dip satellite sleeve"

  - label: songshan-copytrade-poll
    category: order
    order_capable: true
    status: disabled
    disabled_since: "2026-07-27"
    schedule: "weekdays 09:00-13:30, every 5min"
    description: "Copy-trades 凱基松山 (9217) branch signal, ~10万 TWD budget/entry"

  - label: expert-pool-staged-gate
    category: order
    order_capable: true
    status: disabled
    disabled_since: "2026-07-27"
    schedule: "weekdays 09:00/01/05/25"
    description: "Expert-pool staged entry gate (gap→05→25 funnel)"

  - label: detach-gate
    category: order
    order_capable: true
    status: disabled
    disabled_since: "2026-07-29"
    schedule: "weekdays 09:00-13:30, every 5min"
    description: "TW/US detach risk gate — was flag-safe but job wasn't launchctl-disabled until 07-29 audit"

  - label: tmf-channel-poll
    category: order
    order_capable: true
    status: enabled
    enabled_since: "2026-08-05"
    schedule: "KeepAlive worker · interval≈20s in-window · day 08:45-13:45 + night 15:00-05:00"
    description: "TMF Final v1.4.0 PV16 normal/dhwv block（O-anchor）· tmf_channel.causal_engine · session-reuse worker · .env 四鎖"

  - label: momentum-rotation-poll
    category: order
    order_capable: true
    status: enabled
    enabled_since: "2026-08-12"
    schedule: "KeepAlive worker · interval≈1s in-window（2026-08-13 20s→5s→1s，使用者要求；⚠️12檔trades()請求量隨之提高，rate limit風險見下）· weekdays 08:44-13:41"
    description: >
      ⚠️ 2026-08-13 11:04起 ORDER_MOMENTUM_ROTATION_AUTO_SUBMIT改回0（dry-run）
      ——使用者live當天發現4筆交易全虧、追查後發現回測本身的fill-price假設有問題
      （用「訊號當下實際tick價」重跑backtest，勝率從86.9%掉到40.2%、日均從+22.8%
      掉到+1.67%，見reports/research/momentum_breakout_expert_pool_futures_summary.md
      開頭的2026-08-13重大更新章節）——回測宣稱的高勝率絕大部分是量測錯覺，不是
      執行問題，在這個問題被完整重新驗證之前**不要**恢復AUTO_SUBMIT=1，即使明天
      開盤worker照常會跑，也只會是dry-run。當天收工時ledger是halted=True、空手、
      無孤兒部位（AI watch agent判斷「額度錯誤已反覆5輪未解決」觸發，同時使用者
      已知情這個回測發現，主動選擇今天不再嘗試恢復）。

      個股期貨開盤動能12檔單槽位輪動+動態搶佔（momentum-rotation）· 開盤±0.5%突破+
      量能≥1.5x確認進場 · 1%移動停利 · 同檔一天可多次觸發（rearm 0.25%）· 搶佔倍數
      2.0x。2026-08-12 使用者明確決策：2026-08-13起真人真金實盤（LIVE，非dry-run），
      第一階段固定1口，保證金預算11萬。ORDER_MOMENTUM_ROTATION_AUTO_SUBMIT=1（已在
      mini .env 設定，2026-08-13開盤起送真單，11:04起改回0，見上）。安全閥：
      ORDER_MASTER_ENABLED總開關
      （=1）+ 連續進場失敗5次熔斷（max_consecutive_entry_failures）+ 單日虧損
      -3%熔斷（daily_loss_limit_pct）。引擎：src/order/momentum_rotation_order.py
      （reconcile_once狀態機）+ momentum_rotation_signal.py（12檔訊號邏輯）。
      2026-08-13盤前：資料來源從1分K（fetch_1m_bars）改逐筆成交（fetch_1m_bars→
      order/momentum_rotation_tick_marketdata.py::fetch_ticks_as_bars，使用者明確
      要求比照回測simulate_portfolio_day最細刻度）；process_new_bar本身通用不用改，
      等同性驗證見tests/test_momentum_rotation_tick_marketdata.py（真實逐筆資料重放
      比對兩條路徑，過程中抓到並修掉process_new_bar一個跟回測不一致多年的volume
      baseline seeding bug——原本開盤那根K的volume會被錯誤塞進baseline歷史，
      跟回測「空歷史預設1.0」不一致，只影響每檔每天第一個候選訊號的判定）。worker
      輪詢間隔20s→5s，降低trades()可能截斷回傳筆數的風險。⚠️ 兩個沒辦法在今天
      開盤前驗證的殘留風險（見momentum_rotation_tick_marketdata.py檔頭）：
      Fugle/富邦intraday.trades()真實回傳筆數上限未知、回傳欄位名稱是防禦性猜測
      （比照candles/tickers慣例），失敗模式設計成安靜漏訊號而非用錯資料下錯單。
      2026-08-13再修一項：reconcile_once進場/搶佔選擇規則從「本次poll候選裡分數
      最高」改成「嚴格按BreakoutSignal.trigger_time排序、先到先贏」（搶佔則是
      「先到且分數有過PREEMPT_MULT門檻」），對齊回測simulate_portfolio_day逐筆
      掃描的規則——兩者只有「同一次poll內≥2檔不同標的同時合格」才會有差異；拿
      全部75天真實逐筆資料重放過這個情境，0次發生，理論落差消除、經驗機率原本
      就極低。單元測試見tests/test_momentum_rotation_entry_selection.py。
      2026-08-13盤中10:04-10:30真實事故（比前兩個更嚴重）：使用者發現當天4筆
      交易ledger自報ret_pct全部是正報酬（+6.14%加總），但broker權威成交價
      （get_order_results_detail）顯示4筆全部虧損（真實加總-1.82%），3筆連
      正負號都相反——根因是fill/exit_price從來沒用過真實成交價，_enter_position
      存的fill是理論觸發價（long_trigger/short_trigger）、_close_position的
      exit_price是最後觀察tick價/理論停利價，不是broker真實撮合價。這個bug
      也連帶讓daily_loss_limit_pct（-3%熔斷）用錯誤數字計算、可能該觸發時不觸發
      ——比前兩個bug更嚴重，牽涉風控機制本身。修復：新增
      fubon_futopt_orders.fetch_real_fill_price()（查get_order_results_detail
      的details陣列裡status=50那筆的after_price，市價單約780ms才會撮合完成，
      故有限次數重試），_enter_position/_close_position改用真實成交價，
      PositionState/trades_today新增fill_source/exit_source欄位留痕跡
      （"real" vs "theoretical_fallback"，查不到才退回理論價、且會被看到，
      不悄悄冒充精確數字）。已用今天已知的8筆真實成交（4筆進場+4筆出場）逐一
      回歸驗證、8/8比對正確；單元測試見tests/test_fubon_futopt_orders_fill_price.py
      （mock版本，鎖住status=50/10判斷、重試、API例外三種情境）。當天既有的4筆
      trades_today記錄也已用真實成交價人工回溯修正（date-scoped，只影響
      2026-08-13這天的ledger）。另外，10:04-10:11期間AI watch agent也抓到一個
      獨立於前兩個bug的現象：連續多次進場遇到broker「交易額度超過資(財)力證明
      額度」拒絕，判斷「常在搶佔後立即re-entry時發生，疑似保證金釋放延遲，非
      策略邏輯問題」，觸發kill switch暫停——這件事本身沒有code fix（mini帳戶
      總保證金只有30萬NTD，TMF/dayflip-short/momentum-rotation三個live sleeve
      共用同一份、無跨sleeve協調，見memory「mini帳戶總保證金30萬」），是否恢復
      交易需要使用者決定，不是bug。10:36再修一項（使用者要求「研究下單為什麼
      賠錢」後發現的根因）：把當天4筆真實交易的成交價拿去反推理論觸發價，發現
      進場滑價0.5%~2.7%（市價單IOC完全沒有價格保護），若零滑價4筆全部會贏、不
      是全輸——訊號方向邏輯沒問題，純粹是市價單追價太嚴重。改為限價單：新增
      order/momentum_rotation_tick_marketdata.py::fetch_live_quote()（查
      intraday.quote()的Level-1 bid/ask——這個repo沒有真正的Level-2五檔深度資料，
      連tmf_channel_order.py自己下限價單也是用spot±offset而非真實買賣二價，
      2026-08-13使用者確認這個限制後明確選擇用「Level-1+緩衝」逼近）+
      _compute_marketable_limit_price()（buy=ask+spread、sell=bid-spread，
      spread至少0.1%當緩衝下限）+ `_place_market_order`重構為`_place_order`
      （查不到報價才fallback回市價單，標記price_source="market_fallback_no_quote"
      留痕跡）。⚠️改限價單新增的風險：市價單幾乎必成交、限價IOC沒撮合到會直接
      取消零成交——`_enter_position`/`_close_position`都新增「送單成功但查不到
      真實成交紀錄=視為未成交」的檢查（entry不建倉、close不清空部位/不寫
      trades_today，沿用出場「絕不放棄重試」的既有原則），避免ledger出現
      broker端不存在的幻影部位。單元測試見
      tests/test_momentum_rotation_limit_order.py（含「送單成功但沒成交絕對不能
      建立部位」這條最危險的情境）。監控：
      :8770 儀表板「動能輪動」分頁（reports/research/channel_lab/live_v6_sim_server.py，
      com.jackm4.goldenstocks.tmf-sim-server）+ momentum-rotation-watch（見下條，
      08:40-13:45盤中每15分鐘AI異常掃描）。launchctl bootstrap 單獨安裝（未跑整支
      install-launchd.sh——該腳本因既有us-overnight-futures-sync.command缺檔而無法
      完整執行，屬既有問題，非本次新增；scripts/install-launchd.sh 的
      LABELS/TEMPLATES 已補上本job條目供下次修好後的完整安裝使用）。研究脈絡見
      reports/research/momentum_breakout_expert_pool_futures_summary.md（開頭
      「2026-08-13重大更新」章節有修正後定案數字，本檔原先寫的「可信估計約
      3.58%/天」已知無出處、不可信，不要再引用）。
      2026-08-13再推進一項（使用者明確要求「把websocket連上」）：新增
      order/momentum_rotation_websocket_feed.py，資料來源改成「優先訂閱
      futopt websocket 'trades'頻道（12檔共用一個連線）、新鮮度（30秒內收過
      訊息）不足才退回既有REST fetch_ticks_as_bars」，比照TMF sleeve
      tmf_channel_ws_feed.py同一套已在production驗證過的手法（見該檔
      docstring）。'trades'頻道已用momentum_rotation_websocket_trades_shadow.py
      實測確認Speed模式（跟live worker既有session同一個）沒有被擋、訊息格式
      跟REST trades()解析邏輯相容（重用同一組_normalize_tick/_dedupe_same_second）。
      Kill switch：ORDER_MOMENTUM_ROTATION_WS_MARKETDATA_ENABLED（預設0=關，
      跟TMF那支預設開不同——這支只跑過幾分鐘的2檔shadow smoke test，尚未走過
      一整個交易日驗證，比照這個repo「新機制先關閉觀察」的慣例）。單元測試見
      tests/test_momentum_rotation_websocket_feed.py。目前sleeve本身仍是
      dry-run（見上方11:04起AUTO_SUBMIT=0），這項改動不影響是否送真單，只影響
      訊號資料來源的即時性。

  - label: momentum-rotation-watch
    category: monitoring
    order_capable: false  # 自己不呼叫下單API；但可透過kill switch檔案間接觸發
      # momentum-rotation-poll既有的halted強制平倉路徑，見下方說明
    status: enabled
    enabled_since: "2026-08-12"
    schedule: "StartInterval 120s（2026-08-12使用者要求從900s縮短；launcher自濾 weekdays 08:40-13:45，盤中約150次呼叫/天）"
    description: >
      momentum-rotation-poll 盤中即時監看：headless claude -p 讀today's
      logs/intraday/momentum_rotation_*.log + ledger/broadcast json。判準核心原則
      （2026-08-12使用者明確交代）：觸發暫停不是看「有沒有賠錢」，是看「跟預期/
      研究文件描述的行為模式吻不吻合」——遇到沒訓練過、沒料到、程式碼沒有對應處理
      路徑的狀況，即使沒虧錢也當🚨處理；只有監控工具本身的操作性小狀況（log格式/
      暫時性讀檔問題）才允許agent自主排除，src/order/**、config/order.yaml、.env、
      worker重啟一律禁止agent自己動，全部走🚨流程。異常時：1) 寫
      data/research/momentum_rotation_ALERT_<date>.log 2) 寫
      data/order/momentum_rotation_kill_switch.json（date+reason）——
      momentum_rotation_order.py的reconcile_once下一輪poll（≈20秒內）讀到後轉成
      跟halted一樣的強制平倉+擋新進場路徑，AI只負責判斷+寫旗標，實際平倉沿用既有
      _close_position確定性程式碼，不讓AI直接呼叫下單API；旗標以date欄位自然過期，
      不需要刪檔。3) 呼叫scripts/notify_job_result.py寄信（含per-day dedup flag，
      避免每次poll重複騷擾）——worker自己的email alert機制沒有把noop_halted/
      closed_halted納入寄信名單，這是唯一會為熔斷事件通知使用者的管道。
      2026-08-12 使用者明確授權「email+自動kill switch都做」，同日23:xx部署
      （momentum-rotation-poll worker已kickstart重啟載入新碼）。2026-08-12建立，
      取代原本規劃錯誤的「每日14:00跑一次」設計（使用者更正：「監控agent 8:40就要
      一直跑到收盤才對」）。判準SSOT：scripts/launchd/momentum-rotation-watch-prompt.txt。
      launchctl bootstrap+enable 已完成（使用者遠端明確授權）。

  - label: dayflip-short-poll
    category: order
    order_capable: true
    status: retired
    retired_at: "2026-09-02"
    retired_reason: >
      dayflip-futures-short 研究線降級 rejected（四路平行研究，死因互不重疊：訊號層
      無增量 permutation p=0.22~0.50、執行層 28.4% 鎖漲停回補不了吃掉 70~96%、
      52% 報酬來自 08:45 印價、出場層 38 格 Bonferroni 後全滅）。長邊一併收，理由
      較弱屬營運決策（從未持倉、共用同一條訊號管線、replay 損益伴隨資金池 2.84 倍
      超額）。退役時兩邊皆空手（短邊 no_signal、長邊 positions:[]）。
      三層都關：config/order.yaml enabled=false/dry_run=true/auto_submit=false、
      launchctl bootout + disable、plist 移到 ${GOLDENSTOCKS_DATA_DIR}/retired-plists/。
      程式碼與 FROZEN_SPEC_V1.json 一律未動，要研究時仍可手動跑。
      完整判決見 config/research.yaml 同名 topic 與 memory dayflip-short-verdict。
    enabled_since: "2026-08-07"
    schedule: "KeepAlive worker（2026-08-08 起，取代 StartInterval 60s）· interval≈20s in-window · weekdays 08:44-13:41"
    description: >
      個股期貨隔日沖放空 v1（dayflip-futures-short）· 24席分點跳空觸發放空 · 1口/單日單筆/
      保證金硬上限30萬/無停損（研究證實加停損反而變差）· config/order.yaml dry_run=false，
      使用者2026-08-07明確授權跳過研究規格G2 OOS holdout驗證直接上線，且指示不比照其餘
      sleeve的dry-run-first慣例。安全閥：ORDER_MASTER_ENABLED總開關 +
      ORDER_DAYFLIP_SHORT_AUTO_SUBMIT（plist預設=1）+ 保證金/口數/單日單筆三重硬上限。
      引擎：src/order/dayflip_short_order.py（reconcile_once狀態機）+
      dayflip_short_signal.py（候選篩選）。監控：:8770「隔日沖空單」分頁 +
      dayflip-short-watch（見下條，2026-08-13起每2分鐘即時監看+可自動kill switch，
      取代原本09:10/13:50兩次檢查點的唯讀版本）。margin_cap_twd 2026-08-13從30萬
      降到17萬（使用者實際準備資金，見config/order.yaml notes）。
      研究/週度重算見 dayflip-seat-weekly-refresh（另一筆 research entry）。
      2026-08-08：StartInterval 60s 冷啟動（08:44-13:41 每分鐘全新 Fubon 登入，~297次/日，
      跟 TMF 舊架構同一個問題）→ KeepAlive 常駐 worker，重用單一 session（見
      order/dayflip_short_worker_loop.py + dayflip_short_session_pool.py，架構同
      tmf_channel/worker_loop.py）。切換工具：scripts/order/dayflip_short_cutover.sh。
      舊 scripts/order/run_dayflip_short_poll.py 保留供手動測試（不再是 launchd 進入點）。

  - label: dayflip-short-watch
    category: monitoring
    order_capable: false  # 自己不呼叫下單API；但可透過kill switch檔案間接觸發
      # dayflip_short_order.py既有的halted/force_closed路徑
    status: retired
    retired_at: "2026-09-02"
    retired_reason: >
      dayflip-futures-short 研究線降級 rejected（四路平行研究，死因互不重疊：訊號層
      無增量 permutation p=0.22~0.50、執行層 28.4% 鎖漲停回補不了吃掉 70~96%、
      52% 報酬來自 08:45 印價、出場層 38 格 Bonferroni 後全滅）。長邊一併收，理由
      較弱屬營運決策（從未持倉、共用同一條訊號管線、replay 損益伴隨資金池 2.84 倍
      超額）。退役時兩邊皆空手（短邊 no_signal、長邊 positions:[]）。
      三層都關：config/order.yaml enabled=false/dry_run=true/auto_submit=false、
      launchctl bootout + disable、plist 移到 ${GOLDENSTOCKS_DATA_DIR}/retired-plists/。
      程式碼與 FROZEN_SPEC_V1.json 一律未動，要研究時仍可手動跑。
      完整判決見 config/research.yaml 同名 topic 與 memory dayflip-short-verdict。
    enabled_since: "2026-08-13"
    schedule: "StartInterval 120s（比照momentum-rotation-watch）· launcher自濾 weekdays 08:44-13:41，盤中約150次呼叫/天"
    description: >
      dayflip-futures-short 盤中即時監看：headless claude -p 讀today's
      logs/intraday/dayflip_short_*.log + ledger json。判準核心原則（2026-08-13
      使用者明確交代，比照momentum-rotation-watch同一套哲學）：觸發暫停不是看
      「有沒有賠錢」，是看「跟已知劇本（程式碼裡真的有對應處理路徑的情境）吻不
      吻合」——賺錢但不吻合任何已知劇本一樣要暫停。異常時：1) 寫
      data/research/dayflip_short_ALERT_<date>.log 2) 寫
      data/order/dayflip_short_kill_switch.json（date+reason）——
      dayflip_short_order.py的reconcile_once下一輪poll（≈20秒內）讀到後：idle
      轉halted（今天不再進場）、entered立刻強制平倉（不等13:40）。AI只負責
      判斷+寫旗標，實際平倉沿用既有place_futopt_order確定性程式碼，不讓AI直接
      呼叫下單API。3) notify_job_result.py寄信（含per-day dedup flag）。
      2026-08-13 首次真正部署（先前的09:10/13:50兩次檢查點版本雖然範本/文件
      都存在，但從未被裝進install-launchd.sh的LABELS/TEMPLATES、也從未出現在
      launchctl list——查證後確認實際上從來沒有真正跑過，這次是第一次真正安裝，
      不是修改一個原本在跑的東西）。判準SSOT：
      scripts/launchd/dayflip-short-watch-prompt.txt（改prompt不需重裝launchd）。
      對應src/order/dayflip_short_order.py同批新增的kill_switch_reason()：45個
      既有測試全過+3個新情境smoke test（idle→halted、entered→closed_by_kill_
      switch、terminal→no-op，皆dry-run驗證）。launchctl bootstrap+enable 已
      完成（使用者明確授權執行）。

  - label: dayflip-post-dump-long-poll
    category: order
    order_capable: true
    status: retired
    retired_at: "2026-09-02"
    retired_reason: >
      dayflip-futures-short 研究線降級 rejected（四路平行研究，死因互不重疊：訊號層
      無增量 permutation p=0.22~0.50、執行層 28.4% 鎖漲停回補不了吃掉 70~96%、
      52% 報酬來自 08:45 印價、出場層 38 格 Bonferroni 後全滅）。長邊一併收，理由
      較弱屬營運決策（從未持倉、共用同一條訊號管線、replay 損益伴隨資金池 2.84 倍
      超額）。退役時兩邊皆空手（短邊 no_signal、長邊 positions:[]）。
      三層都關：config/order.yaml enabled=false/dry_run=true/auto_submit=false、
      launchctl bootout + disable、plist 移到 ${GOLDENSTOCKS_DATA_DIR}/retired-plists/。
      程式碼與 FROZEN_SPEC_V1.json 一律未動，要研究時仍可手動跑。
      完整判決見 config/research.yaml 同名 topic 與 memory dayflip-short-verdict。
    enabled_since: "2026-08-09"
    schedule: "KeepAlive worker（比照 dayflip-short-poll）· interval≈60s in-window · weekdays 08:44-13:41"
    description: >
      個股期貨隔日沖倒貨後反彈做多 v1（dayflip-post-dump-long）· 同一套24席分點
      T0買超候選（跟dayflip-futures-short相反方向）· rolling_relative_dip滾動
      窗口相對弱勢訊號進場 · 移動停利5%(EOD-only檢查)或最長10交易日出場 ·
      多部位跨日持倉（跟short邊單日狀態機不同）· 共用dayflip-futures-short既有
      300,000 NTD保證金池（單向讀取短邊ledger，見dayflip_post_dump_long_order.py
      的available_pool_ntd()）· 單一標的曝險上限15%(4.5萬)。
      config/order.yaml enabled=true/dry_run=false/auto_submit=true，使用者
      2026-08-09明確指示「程式碼一寫完直接送實單、不比照dry-run-first慣例」。
      安全閥：ORDER_MASTER_ENABLED總開關 + ORDER_DAYFLIP_POST_DUMP_LONG_AUTO_SUBMIT
      （plist預設=1）+ 300,000共用池/15%單股上限/1口硬上限。
      引擎：src/order/dayflip_post_dump_long_order.py（reconcile_once，多部位
      ledger）+ dayflip_post_dump_long_signal.py（rolling_relative_dip即時偵測，
      復用dayflip_short_signal.py的候選篩選）。
      2026-08-09：plist/launcher渲染+launchctl bootstrap安裝完成（使用者遠端
      明確授權執行，因初次安裝動作被 permission classifier 擋下需要人工確認）。
      安裝後驗證：launchctl print 顯示 state=running、pid穩定、last exit
      code=never exited，worker_start log正常寫出interval=60/idle=120，未進入
      crash loop。當天(週日)在窗外idle sleep，預期週一08:44自動進入輪詢。
      跟 dayflip-short-poll 一樣，這個 job 目前也不在 scripts/install-launchd.sh
      的 LABELS/TEMPLATES 陣列裡（手動渲染安裝，非這次新增的缺口，是既有模式）。

  - label: tmf-sim-server
    category: notify
    order_capable: false
    status: enabled
    enabled_since: "2026-08-06"
    schedule: "KeepAlive · always-on"
    description: "TMF paper UI HTTP :8770（live_v6_sim_server · PYTHONPATH=src 走 tmf_channel 引擎）· 不下單"

  # --- Notify-only, no order code path (ABC retired) ---
  - label: buy-signal-radar
    category: notify
    order_capable: false
    status: disabled
    disabled_since: "2026-07-29"
    schedule: "weekdays 09:00-13:30, every 5min"
    description: "Buy-signal radar, notify only (ABC order path retired 2026-07-16); paused as non-分點 clutter"

  - label: sell-signal-radar
    category: notify
    order_capable: false
    status: disabled
    disabled_since: "2026-07-29"
    schedule: "weekdays 09:00-13:30, every 5min"
    description: "Holdings extension advisory, notify only; paused as non-分點 clutter"

  # --- Infra helper (paused because its only clients — the 4 order jobs — are all off) ---
  - label: order-wake
    category: infra
    order_capable: false
    status: disabled
    disabled_since: "2026-07-29"
    schedule: "weekdays 08:50-13:40, every 5min"
    description: "caffeinate to prevent sleep during order-layer polling; no independent function"

  # --- Retired third-party integration ---
  - label: cursor-agent-worker
    category: cloud-agent
    order_capable: false
    status: disabled
    disabled_since: "2026-07-29"
    schedule: "RunAtLoad + KeepAlive"
    description: "Cursor 'My Machines' worker — log showed it never actually ran a dispatched task since 2026-07-24; replaced by Claude Code cloud routine expert-pool-branch-health-check"

  # --- Data collection (price/quote infra, not strategy-specific) ---
  - label: fubon-premarket-quote-collect
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays ~08:28"
    description: "Pre-market quote snapshot via Fubon Neo"

  - label: fubon-intraday-quote-collect
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays ~08:59 onward"
    description: "Intraday quote collection via Fubon Neo"

  - label: futopt-books-collect-stocks
    category: collect
    order_capable: false
    status: enabled
    schedule: "KeepAlive 常駐（獨立於 futopt-books-collect 的第二條 websocket 連線）"
    description: "個股期貨五檔＋逐筆 · 唯讀 · 2026-08-21 建立。2026-09-01 換池：FUTOPT_STOCK_ROOTS 改為 momentum-rotation v2 宇宙 18 檔（聯電 CC 留在指數連線的 CCF 不搬），舊池（12+7）停收。同日二次換池：v2 死亡帶 7 root（CG/DI/GY/DA/QX/PL/QZ，tick 成本 33-64bp 永不可交易）換成池 v3 候選 7 root（LE 玉晶光/OW 環球晶/IX 景碩/GU 全新/NA 穩懋/ND 威剛/QD 台勝科），維持 18 roots 同一條連線。⚠️ 帳號連線上限實測：第三條個股期 ws 連線 09:51 撞 Maximum number of connections reached，勿再開新連線（futopt-books-collect-newpool 已建檔即廢，模板留在 repo 但不得 bootstrap）。凌晨 tickers() 查空時靠 futures_symbol_cache.json fallback（已播種全池 {root}FI6）。與指數核心 8 檔分開跑：合用一條連線時 27×4=108 個訂閱撞上券商上限，當天指數組日盤掉 52%。刻意不在 install-launchd.sh 的 LABELS 陣列，安裝方式見 scripts/launchd/futopt-books-collect-stocks.command 檔頭"

  - label: limitup-fade-nightly
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 22:45"
    description: >-
      limitup-fade 每晚選股信（實單前測第一步 · 2026-09-03 建立 · 唯讀無送單）。
      策略＝T 日漲停鎖死 → T+1 開盤集合競價放空吃遞延買盤（真 OOS 2020~2026-02 每筆
      +0.79% cluster-t=5.07、日均淨 +0.663%、Sharpe 2.43、七年逐年皆正；規則 SSOT＝
      scripts/research/screen_limitup_fade.py 檔頭，2026-09-03 的 biglot 路徑分解獨立
      確認其機制：鎖死組隔夜超額 +112bps 後開盤轉弱）。流程：fetch_disposal_list（條件5，
      失敗不擋）→ screen → 寄 md 名單＋隔日 08:30 人工四確認清單（試撮未鎖／可當沖／券源／
      處置）。22:45 因 twse_mi_index 源晚間才落庫（19:08 實測仍只有前一日）且錯開 22:15/22:30
      FinMind quota；資料未更新時主旨帶 ⚠。開關 .env RUN_LIMITUP_FADE_NIGHTLY（預設 1）。
      standalone 安裝（不在 install-launchd.sh LABELS），見 .command 檔頭。前測裁決：
      累積名單後對照隔日實際開盤（未鎖比率、可當沖比率、容量），驗執行面而非報酬
      （20 天測不出報酬，見 memory limitup-fade-live-prep）。

  - label: limitup-books-collect
    category: collect
    order_capable: false
    status: enabled
    schedule: "KeepAlive 常駐 · 平日 08:55–13:35 自行判窗，窗外 sleep"
    description: "現股漲停候選的五檔委託簿收集 · 唯讀 · 2026-09-02 建立。**走 TWSE MIS 公開 API，不佔富邦 ws 連線額度** —— mini 已有 5 條富邦 ws（tmf-channel-poll／momentum-rotation-poll／futopt-books-collect／futopt-books-collect-stocks／futopt-fill-listener），2026-09-01 09:51 第 6 條撞 Maximum number of connections reached、指數組日盤掉 52%，故本收集器刻意走另一條路。動機：漲停鎖不鎖得住取決於漲停價上「買方排隊量的補進速度」，逐筆成交只看得到隊列被吃掉多少、看不到補進多少；成交側那一半已實測判別力不足（consume AUC 0.34，IS/OOS 一致）。三層輪詢：掃描層每 300s 把 1971 檔宇宙輪一遍、漲幅 >=+6% 進候選名單（只加不減，上限 180）；候選層每 5s 取五檔；同儕層每 15s 取五檔（候選的統計同儕，上限 250，候選永遠優先佔位）。兩層的丟棄數都會 log、不靜默截斷。POLL_SEC=5 是保守值（MIS 是快照服務、公認約 5 秒更新），收集器自己統計重複快照率、每 10 分鐘印一次，第一天收完可用證據調。相同揭示時間+累積量+五檔的快照會去重不寫。請求間隔 0.35s 禮貌節流。輸出 ${GOLDENSTOCKS_DATA_DIR}/cache/limitup_books/limitup_books_YYYY-MM-DD.jsonl（含 lu_bid_qty＝漲停價買方排隊量、tier=cand/peer）。同儕表 _peer_map.json 由 build_peer_map.py 建（近 60 日日報酬相關最高 5 檔、要求相關 >0.3，2275 檔、最高相關中位 0.66），launcher 每 7 天自動重建。⚠️ 建同儕表時 close<=0 會讓 log 產生 -inf、NaN 污染整個相關矩陣（2026-09-03 踩過，已加 assert）。市場別對照表 _market_map.json 首次執行自動探測（2329 檔中 1971 檔在 MIS 上，其餘多為興櫃、本無漲跌停）。刻意不在 install-launchd.sh 的 LABELS 陣列，安裝方式見 scripts/launchd/limitup-books-collect.command 檔頭"

  - label: watchlist-books-collect
    category: collect
    order_capable: false
    status: enabled
    schedule: "KeepAlive 常駐 · 平日 08:55–13:35 自行判窗，窗外 sleep"
    description: "固定觀察清單（高波動 45＝_live_calib.json 的 universe，含台達電 2308）五檔委託簿收集 · 唯讀 · 2026-09-05 建立。與 limitup-books-collect 同一套 TWSE MIS 五檔抓法（import 重用 fetch/parse_depth/market_map），**不佔富邦 ws 連線額度**；差別＝不掃漲停候選、改收一份固定清單，每天記錄各檔在各價位的買/賣掛單量（牆），供「大戶的牆在哪、撐不撐得住」研究——成交側只看得到隊列被吃多少、看不到補進多少，牆只有委託簿看得到（台達電 09-04 收盤快照即證：成交沒發生在 1800，但買五檔在 1800 掛 203 張最厚）。45 檔一次請求即收完（BATCH=45），WL_POLL_SEC 5 秒可達。相同揭示時間+累積量+五檔去重不寫。清單來源：WL_SYMS 覆寫 / WL_FILE / 預設 _live_calib.json。輸出 ${GOLDENSTOCKS_DATA_DIR}/cache/watchlist_books/watchlist_books_YYYY-MM-DD.jsonl（tier=watch）。⚠️ MIS 是 ~5 秒快照，看得到分鐘級的牆厚不厚、撐不撐得住，看不到 5 秒內補牆速度（要更細得走富邦 ws books、佔額度）。刻意不在 install-launchd.sh 的 LABELS 陣列，安裝方式見 scripts/launchd/watchlist-books-collect.command 檔頭"

  - label: pivot-wall-daily
    category: collect
    order_capable: false
    status: enabled
    schedule: "Tue–Sat 06:00（處理前一個日曆日的日盤＋夜盤）"
    description: "轉折處是否有 order book wall 的每日累積 · 唯讀 · 對照組＋波動分層 · ledger 在 reports/research/channel_lab/pivot_wall_ledger.jsonl。刻意不在 install-launchd.sh 的 LABELS 陣列（比照 fubon-*-quote-collect），安裝方式見 scripts/launchd/pivot-wall-daily.command 檔頭"

  - label: branch-tape-prewarm
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 16:30 + 18:30"
    description: >
      分點 tape backfill for POOLS ∪ holdings, so 20:00+ jobs read pure DB。
      2026-08-10 主排程由 18:30 提前到 16:30：當日一次性探測實測 FinMind
      TaiwanStockTradingDailyReport 於 T 日 16:25 發布（15:00 起每 5 分鐘打，
      16:20 尚無、16:25 found）。18:30 保留為安全網——16:25 僅單日單檔觀測，
      FinMind 若偶爾延後，16:30 那輪會撲空。--refresh-days 2 使兩輪皆冪等。
      ⚠️ **涵蓋邊界（2026-08-17 查證）**：本 job 是 **by-stock** 取數——只抓
      POOLS ∪ holdings 的那 ~44 檔，但那 44 檔會帶回**全部 ~820 席**分點。
      所以 `stock_broker_branch_daily` 從 2026-07-17 起，**未被 by-trader 全 tape
      涵蓋的分點每天只有 ~44 列**（僅那 44 檔的視角），不是全市場 tape；
      例如 8440 摩根大通在 2026-08-14 的那 44 檔裡**不含 2330**。這會讓
      「當日 Top-N 買超」在該期間變成從 ~44 檔宇宙挑選，跨期比較須切窗或先 backfill。
      2026-07-16 之前那份 ~50 萬列/日的全市場歷史是**一次性 backfill**
      （synced_at 全為 2026-07-18T01:21Z），不是每日排程，不會自己往前長。
      要補非涵蓋分點的近期資料，手動跑
      scripts/research/backfill_broker_branch_tape.py --branches <slug> --start … --end …。
      每日 by-trader 全 tape 的涵蓋範圍見 ops-console-evening-sync。
      證據與研究影響量化見 config/research.yaml →
      topics.foreign-branch-copytrade-full-sweep.notes。

  - label: tape-bars-backfill
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 22:30"
    description: >
      分點 tape 活躍標的的 stock_daily_bars 價格補檔（scripts/ops/backfill_active_tape_bars.py）。
      2026-08-17 新增。根因：stock_daily_bars 的每日排程來源**只有 ETF 成分股 watchlist**
      （約 258 檔，收盤 sync 每輪回填 7 日滾動窗）；其餘 150~280 檔一直是偶發手動 backfill
      的殘影（synced_at 可見 2026-06-16 / 07-14 / 08-07 三批），跑完就不再更新，所以每日
      有價檔數從 2026-07-13 的穩定 540 退潮到 419。**不是某個 job 壞了，是價格宇宙從來沒有
      排程來源**，這支就是那個缺席的來源。
      為什麼重要：大量訊號腳本用 stock_broker_branch_daily INNER JOIN stock_daily_bars
      算金額，缺價標的會被靜默丟掉、不報錯。已證實吃掉真實訊號 7610 @ 2026-07-30
      （9217 五日買超 1.456 億、淨比 1.000，live watch 完全沒看到）；同一機制也讓研究母體
      會在有人手動 backfill 時事後長大（whale-branch-l1h7 第十輪 n=36→48）。
      預算制每輪 250 檔（依近 3 交易日 tape 買進**金額**排序——2026-08-18 修：原本用股數排序會把
      高價股系統性排到後面，7610 一股 2,425 元、84,577 股＝2.05 億卻進不了前 250，補一輪還在漏）、
      冪等、可重跑；**週五加跑深掃**（--branch-id 對 9217/9661/9227/9801 各補一次 2024-07 起的
      完整歷史，每席上限 300 檔）——日常模式只看近 3 個交易日活躍者，會漏掉「這幾天沒動、
      但研究窗內出現過」的股票；週五深掃把研究母體缺口壓在一週以內，不必等人手動跑。
      可用 TAPE_BARS_DEEP_SWEEP_BRANCHES / _SINCE / _MAX 覆寫；
      補完會附跑 scripts/ops/check_stock_daily_bars_coverage.py 的覆蓋率斷言（唯讀，
      刻意不影響 exit code——backlog 要排幾天，每晚告警只會變雜訊；斷言的正式消費者是
      08:30 mini-schedule 體檢）。22:30 是為了讀得到當日完整 tape 並避開所有 FinMind
      使用者。不透過完整 install-launchd.sh 安裝（手動 bootstrap，比照
      second-disp-oos-accumulate／songshan-follow-watch 模式），避免彈掉常駐的 live
      tmf-channel-poll worker。

  - label: futopt-fill-listener
    category: collect
    order_capable: false
    status: enabled
    enabled_since: "2026-08-18"
    schedule: "KeepAlive 常駐（24h）· 事件驅動，無輪詢"
    description: >
      富邦帳戶層級成交推播 listener（set_on_futopt_filled / set_on_futopt_order /
      set_on_order_futopt_changed）→ cache/tmf_channel/tick_seconds/
      futopt_fill_events_{YYYY-MM-DD}.jsonl。**唯讀觀察，永不送單**；獨立
      session，不碰 live worker 的 session pool。
      2026-08-18 從手動 nohup 改為 launchd 管理。動機：TMF 的市價出場
      （struct_break / trail / max_hold / pre_close_flatten，佔 60 日回放 78%
      的出場）從來沒有在任何地方留下確認成交價，等於在最不該假設零滑價的
      時候假設了零滑價。已量到的滑價 −0.40 pts/邊只涵蓋限價成交（2026-08-13/14
      的 1,742 張委託裡只有 6 張市價單、且都沒配到成交），市價出場的真實
      滑價仍是未知數，而它可能大於整條策略的毛利（見
      reports/research/channel_lab/tmf_true_cost.json）。
      ⚠️ 注意：本 job 只在帳戶真的有成交時才寫檔。TMF 目前
      ORDER_TMF_CHANNEL_ENABLED=0（dry-run），所以**不會**產生 TMF 成交；
      現階段收到的會是 dayflip-short 等其他實彈袖的成交。

  - label: futopt-books-collect
    category: collect
    order_capable: false
    status: enabled
    enabled_since: "2026-08-18"
    schedule: "KeepAlive 常駐（24h）· websocket 推播，無輪詢"
    description: >
      CCF/TMF/EXF/SXF/SOF/SPF 五檔（books）＋逐筆（trades）websocket 收集器
      → cache/{root}_books/ 與 cache/{root}_trades/。**唯讀**；獨立 session。
      2026-08-18 從手動 nohup 改為 launchd 管理，同時修掉一個會污染下游分析的
      資料缺陷：日盤與夜盤兩路訂閱送到同一個 callback、訊息本身沒有任何欄位
      能分辨，收盤那一側會凍結在最後一筆並每次 session 續約（約 58 分）原樣
      重送一次（實測 336,953 列中 492 列是殭屍）。現在每列都帶
      session（由交易所端 book_time 判定 day/night）、book_age_sec、stale
      三個欄位，下游用欄位過濾即可，不必自己逆推。
      實測 TMF 盤口輪廓（見 reports/research/channel_lab/
      tmf_book_microstructure.json）：中位價差 3 點、bid1/ask1 各 3 口、
      top-of-book 存續期中位數 0.12 秒。

  - label: taifex-intraday-snapshot
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 08:46 / 13:46 / 15:01 / 16:46"
    description: >
      期交所 MIS 盤中＋夜盤即時報價（台指期 TXF、聯電個股期 CCF）→ futures_intraday_snapshot。
      2026-08-10 新增。決策規則需要「16:45 當下 CCF 夜盤價」（進場）與「隔日 08:45 日盤開盤價」
      （出場），這兩個時點只能當下捕捉、事後補不回來。資料源比較：FinMind 快照不涵蓋 CCF；
      FinMind tick 只有日盤 08:45-13:44；FinMind daily 只有整場 OHLC。Fubon 可以但 16:45
      在常駐 tmf-channel-poll worker 的窗內（>=15:00），另建 session 可能踢掉它。
      期交所 MIS 是純 HTTP、無 session，是唯一無衝突解。Symbol 後綴 -F 日盤 / -M 盤後；
      前月合約以 CTotalVolume 最大者自動推導（轉倉自動跟上）。表由腳本自建、未 bump
      SCHEMA_VERSION。取代先前的 tx-futures-snapshot（FinMind 版，因不涵蓋 CCF 而退役）。
      2026-09-03 擴充：08:45／13:45 兩個日盤時點自動加抓 biglot 隔夜訊號宇宙的 44 檔個股期貨
      （僅 -F 日盤；代碼動態讀 _live_calib.json 的 fut_code，兩碼者補 F；calib 缺失時退回
      TXF/CCF 不擋原功能；MIS 請求 50 檔一批、0.35s 節流，首批失敗才算 BLOCKER）。
      目的：補「已驗證的隔夜訊號 → 可執行」的最後資料缺口——13:30-13:45 進場基差與
      08:45 出場時點的期現價差，累積 2~3 個月後可直接裁決期貨執行版是否成立。
      2026-09-03 13:45 dry-run 實測 48 筆全到位（含南電 RKF、南亞科 CYF 等）。
      2026-09-04 起 08:46 採集完成即寄「個股期貨開盤信」（45 檔期貨開盤＋隱含跳空 vs 現貨
      昨收，依跳空排序；09:00 biglot worker 另寄同源版）；信件失敗不擋快照；
      開關 .env RUN_FUTOPT_OPEN_MAIL（預設 1，與 09:00 版共用）。

  - label: us-overnight-futures-sync
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 17:30"
    description: >
      ES/NQ 隔夜快照同步（Yahoo ES=F / NQ=F）→ us_futures_overnight_snapshot。
      2026-08-10 新增：該表過去無任何排程、2026-07-13 後停更，而它是 us_tw_* 研究線
      與 16:25 決策面板的輸入。capture_label 09:30~13:30 皆為台股日盤時段，故 13:30
      收盤後即可補齊當日；--tail-days 5 為滾動補檔兼修前幾日缺漏。
      純 Yahoo 抓取，不建立 Fubon session（避免與常駐 TMF worker 搶 token）。

  - label: nq-es-1m-accumulate
    category: collect
    order_capable: false
    status: enabled
    schedule: "daily 08:00 / 20:00"
    description: >
      NQ/ES 1分K滾動歸檔（Yahoo NQ=F/ES=F intraday 1m，只提供~8天歷史，
      parquet封存去重累積）。2026-08-12 新增：tmf-exit-mechanism-search 研究線
      的 H-TXNQ-DECOUPLE-DEFENSIVE-EXIT（台指-NQ脫鉤防禦性出場）需要真實1分K
      重疊史才能驗證live解析度訊號，目前可用天數僅約4天不足以下結論。純研究
      資料累積，不建立 Fubon session、不碰下單邏輯、不影響任何 order layer job。
      腳本：scripts/research/nq_es_1m_daily_accumulate.py。

  - label: taifex-tick-accumulate
    category: collect
    order_capable: false
    status: enabled
    enabled_since: "2026-08-13"
    schedule: "daily 16:30 / 20:00"
    description: >
      TAIFEX（台灣期貨交易所）官方「前30個交易日期貨每筆成交資料」滾動歸檔
      （免費download，Daily_YYYY_MM_DD.zip，涵蓋TX大盤+momentum-rotation
      12檔個股期貨），每日append去重存成CSV，欄位比照既有
      reports/research/expert_pool_futures_tick/*.csv慣例（date/futures_id/
      contract_date/price/volume）。動機：momentum-rotation 2026-08-13研究
      發現Fubon REST intraday.trades()有~50筆上限（可能漏兩次poll間的tick）、
      且SDK完全沒有歷史tick回放API，只能靠手動存的固定快照當歷史資料庫。
      ⚠️ 2026-08-14訂正：上一版這裡寫「REST/WebSocket都沒有Level-2訂單簿
      深度，已查證」是錯的——只查了REST（intraday.quote()確實只有Level-1）,
      沒實測WebSocket。實測發現session.sdk.marketdata.websocket_client.futopt
      訂閱{"channel":"books","symbol":...}會推真正五檔（bids/asks各5檔
      price+size），比照tmf_channel_ws_feed.py既有的候/candles訂閱模式即可
      用。仍然沒有歷史（channel只推「現在」的變動，沒有回放），且盤中實際
      更新頻率／是否逐tick不漏尚未在活躍交易時段驗證過，見memory
      「五檔兩條真來源」。TAIFEX免費資料能解決
      「完整、官方、可長期累積」這兩個問題，但**不含買賣方向欄位**（成交
      數量標記為(B+S)合併量，不分邊）——不是真正的order flow imbalance
      資料，跟現有Fubon逐筆同一個資訊等級，只是不會被50筆截斷、可以無限期
      累積。免費視窗只留前30個交易日，一天不跑就永久漏當天，一天兩次(16:30/
      20:00)降低單次失敗造成永久缺口的機率，比照nq-es-1m-accumulate同一個
      手法。⚠️ TX（大盤台指期貨）因為有夜盤、交易日認定橫跨午夜，依日曆日
      累積時筆數會忽多忽少（資料沒漏，只是分布不均勻）——12檔個股期貨無夜盤
      不受影響。純研究資料累積，不建立Fubon session、不碰下單邏輯、不影響
      任何order layer job。腳本：scripts/research/taifex_tick_daily_accumulate.py。
      2026-08-13同一個job時段內加跑第二支腳本
      scripts/research/taifex_broad_tick_daily_accumulate.py——momentum-rotation
      micro VCP（量縮盤整+趨勢方向一致+爆量）研究在12檔上驗證時撞上樣本量
      瓶頸（4折留一窗口交叉驗證常有3折崩潰到個位數~十幾筆交易，risk-adj
      變成不可信的-inf），需要擴大標的池增加訊號密度。同一份TAIFEX每日zip、
      不用另外下載，只是解析時收下全部326檔個股期貨（商品代號pattern
      `^[A-Z][A-Z0-9]F$`，含意外收穫TMF微型台指期貨），各自存獨立CSV於
      cache/momentum_rotation/taifex_tick_daily_broad/{product_code}.csv，
      跟前者的12檔存檔互不干擾。單日資料量約60萬行、24MB。

  # --- 分點/expert-pool research + digests (the line that stays active) ---
  # installed launchd label is `nightly-expert-digest`; earlier names
  # specialty-expert-pool-watch / winbond-expert-pool-watch merged into it.
  - label: nightly-expert-digest
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 20:00"
    description: "Expert-pool + 松山/新店 buy-side consensus digest, email only (formerly winbond-/specialty-expert-pool-watch)"

  - label: chip-daily-brief
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 21:00"
    description: >-
      籌碼 v4 每日簡報：偏多／偏空各 10 檔 ＋ 今日五分位回顧 ＋ 固定提醒，email only。
      唯讀研究、不下單。跑 scripts/research/run_chip_daily_brief.py，會先補當日
      借券賣出餘額／借券費率／上市＋上櫃全市場日線（分點由 18:30 branch-tape-prewarm 負責）。
      為何 21:00：TWSE 的 TWT93U／t13sa710／MI_MARGN 實測約 20:40 才可取得，21:00 留餘裕。
      休市日靜默跳過（判準＝當日有無日線成交）。開關：.env RUN_CHIP_DAILY_BRIEF（預設 1）。
      ⚠️ 信中名單**不是買賣建議**：可執行邊際（開→收五分位價差）+0.051%/日，
      低於來回成本 0.12–0.15%，照名單交易期望值為負。風險段落 SSOT 在該腳本的 CAVEATS。
      同時把當日結果寫進 chip_score_forward_track（每日彙總）與 chip_brief_list_forward
      （**寄出名單的逐檔紀錄**，含 K=1／K=5 回填）。前瞻設計 2026-09-04 重新凍結：
      舊紀錄追的是「全市場 rank／含 ETF／5.8%」的分數而非寄出的名單、權重曾中途由
      0.25 改 0.50、且只記 K=1（信裡建議的是 K=5）——舊列標記 legacy_hs 保留不併算。
      否證條件 H-CHIP-FORWARD-TRACK／H-CHIP-BRANCH-INCREMENT 見 config/research.yaml。

  - label: chip-loud-accum-track
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 22:15"
    description: >-
      高調吸貨前瞻紀錄（topic chip-loud-accum-forward · 凍結規格 2026-08-28）：
      FinMind by-stock 抓全宇宙（close>=10·20日均量>300k·四碼）分點日報 ~950 req，
      算買賣家數差與 top1/top2_5 集中度，買賣兩側各 30 檔寫 DB 表
      chip_loud_accum_forward，並回填到期事件的 T+1/T+10 outcome。唯讀研究、
      不下單、不寄名單信（失敗才通知）。60 個訊號日後依預註記判準結算
      （NW t>=3 才可談採納、t<1.5 判過擬合）。開關：.env RUN_CHIP_LOUD_ACCUM_TRACK（預設 1）。
      為何 22:15：錯開 21:00 chip-daily-brief 的 TWSE 補檔與 FinMind quota（6000/hr）。

  - label: chip-lot-probe
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 12:50（一次性，13:35 自動退出）"
    description: >-
      盤中單量結構探針（chip-loud-accum-forward 的儀器層）：12:50 Fubon REST 快照
      掃全市場（TSE+OTC 各 1 request）挑漲>=3% 且放量 <=45 檔，開獨立第三條行情
      websocket 訂閱 trades，記錄至 13:35（含收盤集合競價）→ 均張/大單佔比，
      與 22:15 chip-loud-accum-track 的家數差對照，驗證「盤中判別大戶集中買」。
      唯讀、無送單路徑；訂閱預算 45×1 遠低於 108 撞牆教訓。刻意不進
      install-launchd.sh LABELS（比照 futopt-books-collect-stocks），安裝見
      scripts/launchd/chip-lot-probe.command 檔頭。開關：.env RUN_CHIP_LOT_PROBE（預設 1）。
      2026-08-28 smoke 驗證：第三條連線登入/快照/訂閱 ack 全通過。

  - label: futopt-mis-depth-collect
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 08:44（08:45–13:45 常駐輪詢；09:10 / 10:10 為補啟動）"
    description: >-
      v3 45 檔個股期貨的五檔深度，走**期交所 MIS**（純 HTTP POST getQuoteDetail），
      一次請求回 45 筆完整五檔（CBidPrice1-5 / CAskPrice1-5 / CBidSize1-5 / CAskSize1-5
      / CBestBid,AskPrice / SpotID），實測 49 KB。預設每 60 秒一輪。
      **刻意不走 Fubon ws books**：補齊 45 檔要 34 root × 4 訂閱 = 136 個訂閱、得再開
      3 條 ws 連線；mini 已掛 11 個 job（含唯一 live order-capable 的 tmf-channel-poll），
      而 futopt-books-collect-stocks 檔頭記著 2026-08-21「108 訂閱擠一條連線 → 日盤掉
      157/300 分鐘（52%）」的事故。MIS 不佔連線額度，零風險。
      用途：算個股期貨真實價差與深度 → 決定「大戶未平倉訊號改做個股期貨」能否過成本
      （台指期來回 ≈2 bps、v3 契約價差中位 33 bps、最緊的 14 bps；規則 A/C 兩平 15 bps、
      E 兩平 24 bps —— 差距就在這裡，必須有實測價差才算得出淨值）。
      輸出 ${GOLDENSTOCKS_DATA_DIR}/cache/futopt_mis_depth/mis_depth_{date}.jsonl。
      唯讀、無送單路徑。刻意不進 install-launchd.sh LABELS，安裝見
      scripts/launchd/futopt-mis-depth-collect.command 檔頭。
      開關：.env RUN_FUTOPT_MIS_DEPTH（預設 1）· MIS_DEPTH_INTERVAL / MIS_DEPTH_UNTIL。

  - label: biglot-bucket-eod
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 15:40 / 20:10（兩次為降低單次失敗造成永久缺口）"
    description: >-
      收盤後把當天「大戶未平倉損益 × 等量成交格」落成 CSV，供 25 個交易日後的檢定。
      規格（2026-09-06 鎖定，收滿 25 個交易日前不調參數）：等量成交值格（監測清單自己的
      成交值，13:25 後不進時鐘、**秒級解析度**）· 大戶＝單筆 >= NT$5,000,000 且可判方向 ·
      **每格歸零**加權平均成本、只取未實現 · 執行標的台指期（來回成本 2 bps）· **持有版**。
      已定案、非待驗項目（結構性、與樣本無關）：①秒級解析度 —— 分鐘版 N=60 只切得出 57~58
      格、每格佔比 std 0.37~0.43%，秒級是剛好 60 格、std 0.01~0.02%；分鐘版曾讓 N=30 的 A
      規則從 +4.73 虛增到 +7.31 bps/格。②持有版 —— N=60 進出版每日換邊 16.5~21.5 次、
      持有版 3~6 次，成本差 20~35 bps/日。
      待驗組合 6 格（Bonferroni 門檻 |t| > 2.6）：N ∈ {30, 60} × 規則 {A, C, E}。
      樣本從 2026-09-08 起算 25 個交易日；2026-09-03 / 09-04 兩天**不列入檢定**
      （那兩天用舊 biglot 訂閱清單收的，跟 v3 只重疊 19 檔，只當管線驗證）。
      宇宙 2026-09-06 因修 bug 換過：流動性門檻由「口數」改為「名目成交金額」——
      小型契約 1 口只有 100 股，用口數挑會固定選到小型契約（45 檔裡錯了 16 檔）。
      A＝連續 k=3 格未實現 > 0；C＝連續 k=3 且 bps[t] > bps[t-1]；E＝連續 k=3 且
      bps[t] > 前兩格平均。「越賺越多」一律用 bps 判斷不能用金額（金額被部位大小主導）。
      D（bps 單調遞增）已剔除：N=30 持有淨 +51.7、N=60 −17.3，兩個 N 上翻號。
      N=40 只當內插檢查、不進結論表。全部可從輸出 CSV 事後重算，加變體不必改收集。
      證偽檢查：若 25 天後 A/C/E 在 N=30 與 N=60 上結論相反＝參數過擬合，整條線收掉。
      輸入＝biglot-live-watch 落的 cache/biglot_live_watch/raw_{date}.jsonl（沒有就跳過）；
      另補當天 TX 逐筆（FinMind，1 request，失敗不擋）。
      輸出 ${GOLDENSTOCKS_DATA_DIR}/data/cache/biglot_bucket/bucket_reset_n{30,40,60}.csv（依日期去重，可重跑）。
      唯讀、無送單路徑。刻意不進 install-launchd.sh LABELS，安裝見
      scripts/launchd/biglot-bucket-eod.command 檔頭。開關：.env RUN_BIGLOT_BUCKET_EOD（預設 1）。
      ⚠ 目前只有 2 天樣本、掃過 20+ 組參數、有效獨立樣本＝2，**不是結論**；此 job 的唯一目的
      是把樣本收到能檢定的程度。

  - label: biglot-live-watch
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 09:00（一次性，13:32 自動退出）"
    description: >-
      大戶盤中佈局監看：09:00 起訂閱 45 檔 trades 累積「大戶淨主動買」，
      12:00 / 13:00 / 13:30 各寄一封多空前十名，收盤後再一封。
      大戶定義＝單筆張數 >= 該股 P99（固定金額門檻對高價股失效：500萬對大立光僅 0.7 張）；
      內外盤用 Lee-Ready；已排除 isTrial 試撮與 isClose 收盤競價。
      訊號預測隔日開盤跳空，非當日走勢（純盤中版本實測 +1.9~+9.6 bps 全不顯著）。
      **必須 09:00 起算**：全場 IC +0.151，只取 12:00-13:00 掉到 +0.068 且對全場零增量。
      宇宙＝PIT tick 宇宙 ∩ 個股期貨日均量>=500 口（兩腿皆可做；期貨放空無平盤下限制），
      定義於 ${GOLDENSTOCKS_DATA_DIR}/data/cache/pit_universe_tick/_live_calib_v3.json
      （2026-09-05 起；產生器 scripts/research/build_hivol_futures_universe.py，可重現。
      舊 _live_calib.json 的 vol20 欄位混了兩種量、且對 8 檔標的用到已萎縮的舊期貨契約，
      詳見該腳本檔頭。launcher 以 BIGLOT_CALIB 指定，檔案不存在時 fail-safe 回退舊檔）。
      唯讀、無送單路徑；訂閱預算 45×1。刻意不進 install-launchd.sh LABELS（比照
      chip-lot-probe），安裝見 scripts/launchd/biglot-live-watch.command 檔頭。
      開關：.env RUN_BIGLOT_LIVE_WATCH（預設 1）。
      ⚠ 個股層級排名經 IC/命中率/期望報酬三種度量檢定皆不可持續（IS→OOS 相關 ≈0），
      名單要整組用；成本是生死線（兩腿各 1 跳動已 45 bps，回測 OOS 毛 +91~+120 bps）。
      2026-09-03 起信尾附「留倉風險儀表」：VIXTWN 一年分位 × 全市場跌停家數（REST 快照
      2 request）→ 明日個股左尾對照（11 年 n=2,813 實測；118 檢定 null 後唯二存活的風控儀表，
      見 memory index-gap-prediction-118-tests-null）。大戶門檻同日改為固定 500 萬（自適應 P99
      經 IS-only 重估證實過擬）。
      2026-09-03 起信中加印「大戶 ∩ 散戶 同向」觀察組（多空各 5 檔，不取代主名單）：
      散戶＝單筆 1 張；閘門＝大戶買≥P50 ∩ 散戶買≥P67（空方鏡像）。2026-09-04 加「昨日分」欄
      （前一交易日 大戶名次+散戶名次，收盤落 concord_score.json、隔日啟動載入；>1.33 標『連』=
      昨也同向買、<0.67 標『逆』）：回測連續同向買隔日 +92bps vs 突發 +46，供判斷延續或突變。回測 100 檔 ×126 日、
      13:00 截點→隔日開盤：同向格 +62.4bps/日（t=4.66、勝日 66%、區塊自助 P(>跨價差成本)=90%），
      而「大戶買、散戶賣」僅 +6.2bps（t=0.73）；中間層獨立係數 t=0.50 可忽略。
      ⚠ 每日僅 3~5 檔、SD 為主名單 2.4 倍、樣本僅 126 日 —— 先觀察對照，勿據此放大部位。
      同日另修三處（皆不動 A 名單的排序與門檻）：
      (1) 回測基準改寫為經驗證的執行路徑「訊號 09:00-13:00 → 今日 13:30 收盤集合競價進
          → 隔日 09:00 開盤集合競價出」（兩端單一價、不跨價差）：多腿 +50.5bps/日、t=6.81、
          Sharpe 9.40、勝日 73%、逐月 6/6 正、區塊自助 95%CI [+33,+59]。舊的「+91~120bps、
          扣 2 跳動打平」是 13:20-25 VWAP 進／09:00-05 VWAP 出的舊路徑，已不適用。
      (2) 各時點加註用途：12:00 觀察／13:00 預告／13:30 ★可執行（現貨收盤競價已撮合、
          訊號定型，個股期貨尚有 13:30-13:45 可下單）／收盤 對帳。
      (3) 金融保險族群標「!」並附警語：多腿超額實測 -58.4bps(t=-2.43)、族群內排名亦無效
          (t=-0.06)；成因為 20 日波動僅 196bps（全宇宙 448bps 的 44%），低波動股要擠進前 20%
          需異常大的淨流而該流量推不動價，入選即誤報。只標記不改排序。
      同日修一個 bug：_load_vixtwn 未對 market_vix_daily 去重（finmind/computed 雙來源，
      2026-03-02~09-02 共 117 個重複日期），使「一年分位」實際只涵蓋 7 個月且偏高波動區間。
      實測 2026-09-02 分位由 2.0% 修正為 38.9%（視窗中位 36.3 → 29.0）。今日兩者同屬低檔帶，
      但分位跨越 P50/P80 邊界時會給出錯誤的留倉建議。
      2026-09-03 深夜補驗（決定性降級）：上述回測基準與同向格的帳面 alpha 主要是漲停鎖死代理
      —— 多腿 Q5 有 31.3% 收盤鎖漲停（收盤競價買不到），該批獨佔 +112bps；同向格入選者更有
      55.7% 鎖死。跳過鎖死的實戰模擬：A 名單 +15.7bps/日(t=1.71, CI[-2,+35], P(>27bps)=13%)、
      同向格 +13.6(t=1.01, 月正3/6)。兩份名單自此定位為觀察性報告，非可執行 alpha；信中警語
      已同步。訊號本身統計為真（無過濾 t=7.3），但可成交的殘餘未達採納門檻。
      2026-09-03 起加寄兩封早盤觀察信（09:15／09:30 · paper-trade 前瞻紀錄 · 不下單；
      09-04 起第二封由 09:35 改 09:30 與回測截點對齊，並加寄 09:00「個股期貨開盤信」——
      讀 taifex-intraday-snapshot 的 08:45 快照，列 45 檔期貨開盤價與隱含跳空〔期貨開盤
      vs 現貨昨收〕，作為昨日 13:30 名單的出場預覽；快照缺料寄警示不擋流程；
      開關 .env RUN_FUTOPT_OPEN_MAIL 預設 1）：
      三欄各前 8（①A原始 ②同業相對 ③A+今晨跳空），皆排除金融、跳過已鎖漲停、附現價與
      距漲停%。凍結裁決判準（09-03 深夜修訂為兩層，當時前瞻資料尚未開始累積）：≥40 交易日後，
      日均毛 ≥+35bps 且勝日 ≥60% → 續命；+15~+35 且 t≥1.5 → 延長至 80 日再裁；其餘收攤
      （40 日 SE≈17bps，單一門檻對真值 +25 檢定力不足；出場基準＝隔日 09:00 開盤集合競價）。
      live(Fubon Lee-Ready) vs 回測(FinMind TickType) 同日八檔對帳：Spearman 0.95、
      平均絕對差 3.7/全距 61，唯寬價差股 8046 差 20.8 點且散戶 imb 翻號 ——
      跳動 >35bps 者名次僅供參考（信中已加註）。裁決日追加兩項零改動檢定（事前登記）：
      (a) 13:30 信空方名單的「空腿 vs 41檔籃子」前瞻超額——回測 +40.4bps/日(t=5.61,月正6/6)
      但僅能以籃子為基準衡量；TXF 實際對沖經真 TAIEX 驗證會把 +46 打成 -46（熱門籃子隔夜
      溢價 ~50-90bps/日非指數可對沖），故此項純驗訊號、非可執行宣稱。(b) 空腿×RRG極端象限
      條件化（三次耳語 t≈1.9-2.1，nowcast 版翻負，預期死）。回測參考（09:30版/126日）三欄 +29.4/+39.1/
      +33.3 bps/日、後半 +15.6/+14.6/+29.7、成本≈27bps —— 期望值按後半看。昨收由
      stock_daily_bars 唯讀載入（失敗時跳空欄缺、不擋信）。

  - label: vixtwn-daily-sync
    category: collect
    order_capable: false
    status: enabled
    schedule: "weekdays 20:30"
    description: >-
      VIXTWN/VIX 日頻同步：sync_vixtwn.py（官方 finmind TaiwanOptionVix + Yahoo ^VIX，
      --days 7 自動補洞）＋ sync_vixtwn_computed.py（TXO 自算備援，近兩月，與官方相關 0.989）。
      根因：sync_vixtwn.py 檔頭寫「可排程」但從未被排程，2026-08-16 後停更 20 天，
      2026-09-03 留倉風險儀表上線時發現（陳舊值 30.22 vs 真值 26.10，分位判定會失真）。
      下游消費者：biglot-live-watch 的留倉風險儀表、TMF VIXTWN gate、morning_gate_brief。
      standalone 安裝（比照 chip-lot-probe），見 scripts/launchd/vixtwn-daily-sync.command。
      開關：.env RUN_VIXTWN_DAILY_SYNC（預設 1）。

  - label: second-disp-expert-pool-watch
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 20:35"
    description: "Disposition-stock expert-pool copytrade watch, T0 filter, email only"

  - label: songshan-follow-watch
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 21:00"
    description: >
      Observe-only：每個交易日固定記錄 9217（凱基松山）跟單訊號的觸發與
      near-miss，不下單、不建立 Fubon session。腳本
      scripts/research/run_songshan_follow_watch.py --branch songshan
      --no-refresh --bootstrap-only；報告寫入
      reports/research/branch-footprint-screen/ab58_xMega_copytrade/watch/
      songshan_YYYYMMDD.md（TRIGGERED / no_signal 兩種都寫），state 在
      ${GOLDENSTOCKS_DATA_DIR}/data/scratch/songshan_follow_watch_state.json。
      建立動機（2026-08-17）：songshan-copytrade-poll 下單 job 已於
      2026-07-27 disabled，此後沒有獨立的 point-in-time 留痕，研究上
      「累積樣本外交易」零進度；事後重算永遠會被質疑是看著結果挑窗口。
      排 21:00 是因為 9217 的分點 tape 由 20:00 nightly-expert-digest／
      20:35 second-disp-expert-pool-watch 先補完，21:00 讀得到當日完整
      資料，也避開 FinMind rate limit 撞車（故帶 --no-refresh）。
      預設不寄信（--bootstrap-only）——20:00 digest 已把同一份 9217 內容
      寄出，重複信無意義；要獨立寄信在 .env 加
      SONGSHAN_FOLLOW_WATCH_EMAIL=1。job 失敗仍會發告警信。
      註：nightly-expert-digest 也會寫同一個報告檔路徑，本 job 是刻意的
      冗餘 PIT 記錄器（20:00 那支若整體失敗，21:00 仍會補上留痕）。

  - label: expert-pool-chart-digest
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 20:05"
    description: "HTML chart digest for expert-pool hits; skips send if nothing hit"

  - label: holdings-branch-sell-monitor
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 20:10"
    description: "Holdings × expert-branch net-sell warning, email only"

  - label: second-disp-oos-accumulate
    category: research
    order_capable: false
    status: enabled
    schedule: "weekly, Sat 08:00"
    description: "Disposition-stock OOS sample accumulation for expert-pool validation"

  - label: dayflip-seat-weekly-refresh
    category: research
    order_capable: false
    status: retired
    retired_at: "2026-09-02"
    retired_reason: >
      dayflip-futures-short 研究線降級 rejected（四路平行研究，死因互不重疊：訊號層
      無增量 permutation p=0.22~0.50、執行層 28.4% 鎖漲停回補不了吃掉 70~96%、
      52% 報酬來自 08:45 印價、出場層 38 格 Bonferroni 後全滅）。長邊一併收，理由
      較弱屬營運決策（從未持倉、共用同一條訊號管線、replay 損益伴隨資金池 2.84 倍
      超額）。退役時兩邊皆空手（短邊 no_signal、長邊 positions:[]）。
      三層都關：config/order.yaml enabled=false/dry_run=true/auto_submit=false、
      launchctl bootout + disable、plist 移到 ${GOLDENSTOCKS_DATA_DIR}/retired-plists/。
      程式碼與 FROZEN_SPEC_V1.json 一律未動，要研究時仍可手動跑。
      完整判決見 config/research.yaml 同名 topic 與 memory dayflip-short-verdict。
    schedule: "weekly, Fri 21:30"
    description: >
      隔日沖策略（dayflip-futures-short v1，見
      reports/research/branch-footprint-screen/dayflip_gapup_short/FROZEN_SPEC_V1.json）
      的24席 flip 分數週度重算 + 近3月/近1月漂移監控 + 週一候選標的清單。
      唯讀，不下單、不自動修改 FROZEN_SPEC_V1.json；報告寫入
      reports/research/branch-footprint-screen/dayflip_gapup_short/weekly_reports/。
      2026-08-07 建立，不透過 install-launchd.sh 陣列（手動 bootstrap，比照
      second-disp-oos-accumulate 模式）。

  - label: capitulation-oos-accumulate
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 19:00"
    description: "投降抄底 forward OOS ledger (capitulation_forward_ledger.py); accumulates out-of-sample events to break the single 2026 regime limit. Read-only DB, email only, 不下單. Added 2026-08-01 (commit ba3e198)."

  - label: crash-thermometer-daily
    category: research
    order_capable: false
    status: retired
    retired_since: "2026-08-01"  # research 31-35% discriminative power (below chance); retired per user
    schedule: "weekdays 09:00"
    description: "8-branch weighted crash thermometer + consensus, email. NOTE: 2026-07-27 research found 31-35% discriminative power on fresh events (below chance) — kept running for reference, not a validated signal."

  - label: morning-gate-brief
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 08:25"
    description: "台股盤前定調簡報 (src/morning_gate_brief.py)：yfinance 抓 NQ/ES 盤前隔夜 → 迴歸預測台股開盤缺口 (缺口% ≈ 0.23 + 1.06×NQ隔夜%, 55日 corr 0.79/命中73%) + VIX/VIXTWN regime。只定調開盤缺口 (研究證實開盤後首時段 corr −0.09、雜訊勿追殺)。不下單。寄信預設關 (RUN_MORNING_BRIEF_EMAIL=1 才寄)。由 scripts/install-wantgoo-loop-launchd.sh 安裝。研究依據 src/research/backtest/us_tw_vix_gate_study*.py。Added 2026-08-03。"

  - label: wantgoo-loop-daily
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 20:15"
    description: "玩股網每日情報迴圈: fetch 白名單6位作者新文→落庫(blog_posts)→記分板 score 隔日驗證作者call→擷取(有ANTHROPIC_API_KEY則API萃取, 無則匯出 bundle 供 Claude session)。不下單。git-tracked + 正式安裝於 scripts/install-wantgoo-loop-launchd.sh（2026-08-01起，取代先前的scp暫時部署）。記分板 = data/wantgoo_loop.db author_predictions。設計見 docs/wantgoo-intelligence-audit.md §10。"

  - label: chip-macro-tracker-daily
    category: research
    order_capable: false
    status: enabled
    schedule: "weekdays 18:00"
    description: "籌碼→大盤 風控燈號 daily_tracker: refresh panel + 6燈(MA200閘門/外資期貨z60/融資斷頭底/VIX gate/rev傾斜/夜盤) + 資料新鮮度gate + 隔日驗證 backfill_outcomes + ops-console 推送. 不下單. 把 Book branch research/dashboard-completeness 的 modern tracker overhaul(MA200/margin-C3/VIX/rev)首次上生產(取代 mini main 的舊版); 會更新公開 ops-console 的 chip_macro payload。2026-08-01起 git-tracked + 正式安裝於 scripts/install-wantgoo-loop-launchd.sh（取代先前的scp暫時部署）。⚠️ KNOWN BUG (2026-07-31, unfixed): daily_tracker.py's PANEL_DIR hardcodes ROOT/data instead of routing through GOLDENSTOCKS_DATA_DIR — same hardcoded-path class as the 4 scripts fixed 2026-07-31; now that this branch is merged into main it's fair game to fix in a future round."

  # --- Live TA / website feed ---
  - label: ops-live-ta-poll
    category: webfeed
    order_capable: false
    status: retired
    retired_since: "2026-08-01"  # in install-launchd.sh RETIRED_LABELS; python kept, plist removed
    schedule: "weekdays ~08:50 trigger, loops to 13:35"
    description: "Live holdings TA → ops.live_ta (Supabase); single Fubon login per poll cycle"

  - label: live-ta-kbar-sync
    category: webfeed
    order_capable: false
    status: retired
    retired_since: "2026-08-01"  # in install-launchd.sh RETIRED_LABELS; python kept, plist removed
    schedule: "weekdays 14:00"
    description: "Daily stock_kbar_1m gap-fill for today's dynamic Live TA universe (added 2026-07-28)"

  - label: ops-console-evening-sync
    category: webfeed
    order_capable: false
    status: enabled
    re_enabled_since: "2026-08-18"
    schedule: "weekdays 20:40（Supabase 推送已關閉，只跑本地 tape/panel）"
    description: >
      ops.snapshots / sleeve / holdings sync to Supabase for the website。
      **2026-08-18 重新 enable，但 Supabase 推送維持關閉**（`.env` 的
      `RUN_OPS_CONSOLE_PUSH=0`，launcher 內三個推送步驟會 SKIP）。
      恢復原因：2026-08-17 整支停掉後實測 8 席 by-trader 全市場 tape 當天就崩
      ——1480 從 1,517 檔掉到 11 檔、1650 1,368→11、1560 1,056→8、5850 1,004→11、
      984K 753→11，24 席高沖席合計覆蓋 1,103→490 檔。而該 tape 是分點籌碼研究的
      唯一每日來源。**這支 job 的價值在前半段（本地 tape/panel），不在後半段（Supabase 推送）**，
      兩者現在由 RUN_OPS_CONSOLE_PUSH 分離。
      ---- 以下為 2026-08-17 停用時的紀錄（供對照）----
      **2026-08-17 曾整支 launchctl disable**：使用者不再使用 Supabase、要停掉
      Pro Plan $25/月扣款；並在被明確告知「會一併斷掉 8 席全市場分點 tape」後
      選擇整支斷（該 8 席多為外資分點，跟單研究已判定全無 edge，見 research 線）。
      **停掉這支等於同時停掉四件事**（不只是 Supabase 推送）：
      (1) backfill_broker_branch_tape.py --universe-json
          crash_thermometer_panel_universe.json —— 8 席 by-trader 全市場 tape 的
          **唯一每日寫入者**（1650 瑞銀／1560 港商野村／1480 美商高盛／1360 港麥格理／
          984K 元大館前／1261 宏遠民生／5850 統一／585c 統一仁愛）。要補回來：
          `PYTHONPATH=src .venv/bin/python scripts/research/backfill_broker_branch_tape.py
          --trader-id <id> --start <date>`（FinMind 有歷史，非不可逆）
      (2) run_market_crash_thermometer_dashboard.py —— 溫度計面板不再更新
      (3) sync_stock_market_daily.py 的窄版呼叫（daily-sync 的全量版仍在跑，不影響）
      (4) build_stage_heatmap_1y.py
      註：9217 凱基松山的全 tape 由 20:00 nightly-expert-digest 寫入，**不受影響**；
      其餘所有分點仍有 branch-tape-prewarm 的 ~44 檔視角。
      launcher 內的 Supabase 推送三步另有 `RUN_OPS_CONSOLE_PUSH`（mini 設 0）保護，
      即使日後有人重新 enable 這支 job 也不會再打 Supabase。
      ⚠️ **副作用（2026-08-17 查證）**：本 job 順帶是目前唯一還在跑的
      **by-trader 全市場分點 tape** 寫入者——launcher 內先跑
      backfill_broker_branch_tape.py --universe-json
      reports/research/branch-footprint-screen/crash_thermometer_panel_universe.json
      --start -5d --force（原意是餵崩盤溫度計）。因此**只有那份 universe 的 8 席**
      有每日全 tape：1650 瑞銀／1560 港商野村／1480 美商高盛／1360 港麥格理／
      984K 元大館前／1261 宏遠民生／5850 統一／585c 統一仁愛
      （2026-08-14 實測列數 1368／1056／1517／166／…／1004；synced_at 12:40Z＝20:40 台北）。
      另有 9217 凱基-松山 亦為全 tape（622 檔、synced_at 12:00Z＝20:00 台北），
      寫入者未逐一比對到 launcher。**其餘所有分點只有 branch-tape-prewarm 的 ~44 檔視角。**
      要新增／移除全 tape 分點，改那份 universe JSON 即可，不必動 launcher。
      註：crash-thermometer-daily（09:00）已 retired、plist 已卸載，但其 launcher
      仍留在 Application Support（孤兒檔，不會被觸發）。

  - label: daily-sync
    category: ingest
    order_capable: false
    status: enabled
    schedule: "weekdays 16:35"
    description: >-
      Full scripts/daily_sync.sh (not --holdings-only) — constituent stock
      market+institutional (sync_stock_market_daily.py, gated by
      RUN_STOCK_MARKET_SYNC=1) + chip daily + ETF holdings + tech risk +
      RUN_*-gated RRG/VCP/Lens/Supabase steps per .env。
      ⚠️ **2026-08-18 踩到的坑：關掉「寫 Supabase」的旗標不等於關掉「讀 Supabase」。**
      斷開 Supabase 後 daily-sync 仍然 exit=1，查出兩個漏網：
      (a) `RUN_STRATEGY_PERF_SYNC` —— 獨立於 RUN_SUPABASE_* 的另一個旗標，
          跑 226s 後 404（stock_research.strategy_performance_yearly 已不存在）；
      (b) `RUN_STOCK_DAILY_LENS` —— 更隱蔽：lens **讀** Supabase 是它核心邏輯的一部分
          （run_stock_daily_lens.py → stock_daily_lens.publish_stock_daily_highlight →
          build_stock_daily_lens_rows → supabase_lens_sync.load_supabase_highlight_for_date），
          **完全不受 `RUN_SUPABASE_LENS_SYNC` 控制**。關掉寫、讀還是會 404。
      兩者已於 2026-08-18 設為 0。教訓：切斷外部依賴時，要 grep 的是**該依賴的 import 鏈**
      （`grep -rl supabase src scripts`），不是只找名字裡有該依賴的旗標。 Added 2026-08-10:
      previously this pipeline was documented in CLAUDE.md as a manual
      "常用指令" (scripts/1630收盤雷達.command only covers --holdings-only,
      a narrower path) with no launchd automation — found stock_daily_bars
      (which dayflip-short/-long build_candidates() depends on for pricing
      branch-buy candidates) was only getting a 5-stock watchlist subset via
      ops-console-evening-sync's narrow sync_stock_market_daily.py call,
      not a real full sync; 192/251 (76%) of the individual-stock-futures
      universe had zero price data on a day nobody happened to run it
      manually. Also fixed a latent bug in daily_sync.sh: its bash-level
      $DB var was hardcoded to ${ROOT}/data/stocks.db (a 0-byte file,
      disconnected from the real stock_db.DATA_DIR-resolved production
      DB) — same PROJECT_ROOT-vs-DATA_DIR debt pattern CLAUDE.md warns
      about, just in bash instead of Python.
      Also runs scripts/research/refresh_dayflip_futures_daily_cache.py
      (found same night: build_candidates()'s ADV liquidity gate reads
      futures_daily_cache.json, which was only refreshed weekly by
      dayflip-seat-weekly-refresh — the fail-closed design silently
      excluded every candidate on any day nobody manually refreshed it,
      confirmed by a real case where 0-candidate flipped to 12 after a
      manual refresh; more severe than the stock_daily_bars gap since it
      zeroes out the whole candidate pool, not just pricing).

  # --- Local AI agent job (headless Claude Code, not a Python pipeline) ---
  - label: mini-schedule
    category: health
    order_capable: false
    status: enabled
    schedule: "daily 08:30"
    description: >-
      mini daily data health check — headless `claude -p` (sonnet,
      --dangerously-skip-permissions), read-only on stocks.db, writes
      goldenstocks-data/data/research/daily_health_<date>.md. Moved into repo
      version control 2026-08-02 from goldenstocks-data/scripts/mini_schedule.sh
      (old hand-installed label com.goldenstocks.mini retired). Criteria SSOT:
      scripts/launchd/mini-schedule-prompt.txt. cwd is GOLDENSTOCKS_DATA_DIR so
      that directory's CLAUDE.md acts as the agent's guardrail.

# --- Cloud agents (not mini launchd — Claude Code scheduled routines, separate system) ---
cloud_routines:
  - name: daily-foreign-rotation-commentary
    schedule: "weekdays 21:30 Asia/Taipei"
    order_capable: false
    description: "Reads Supabase rotation/risk snapshots + web news, writes AI market commentary back to ops_snapshots (kind=commentary) for the website"

  - name: expert-pool-branch-health-check
    schedule: "weekdays 21:00 Asia/Taipei"
    order_capable: false
    description: "Read-only data-freshness health check on ops.* (holdings/snapshots/digests/live_ta), emails a report. No local/git access — Supabase MCP + Gmail MCP only."

  - name: detach-gate-risk-health-check
    schedule: "daily 21:30 Asia/Taipei (found 2026-08-01, created 2026-07-30, previously undocumented)"
    order_capable: false
    description: "Read-only freshness check on ops_snapshots kind=risk (Detach Gate payload) — catches the specific 'outer asof advances but inner as_of/session_date stuck' stale-poller failure mode seen 2026-07-28~30. Only pushes a notification on 🔴 (stuck); stays quiet on 🟢/🟡."

  - name: 台股融資盤後監測
    schedule: "daily ~21:40 Asia/Taipei (found 2026-08-01, created 2026-07-30, previously undocumented)"
    order_capable: false
    description: "Pulls TWSE official margin-balance data post-market, judges whether the ongoing margin-call cascade (融資踩踏) is winding down or continuing, for the 7 currently-held stocks concentrated in substrate/packaging + passive components. Read-only, no Supabase writes."

# --- Confirmed retired, files removed from repo 2026-07-29 (not just paused) ---
# evening-holdings, morning-holdings-brief, intraday-exit-gate, intraday-open/midday/1300-digest,
# rrg-mono-intraday-watch, vcp-funnel-specs, minervini-sepa-basket, mutual-fund-disclosure-watch,
# weekly-deep, c18acc-extension-overlay, order-chase-open, etfedge-import,
# specialty-expert-pool-watch / winbond-expert-pool-watch (merged into nightly-expert-digest).
# See git log (2026-07-29 commits) for why each was retired.

# --- Completed 2026-08-01: GitHub repo renamed etfstrategy -> goldenstocks, mini's launchd
# label prefix renamed com.jackm4.etf.* -> com.jackm4.goldenstocks.* (all ~23 jobs), repo
# checkout moved to ~/goldenstocks on both Book and mini, website moved to ~/goldenstocks-web.
